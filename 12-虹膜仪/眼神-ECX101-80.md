# 虹膜仪 - 眼神 ECX101-80

## 文档版本

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| V1.0 | 2026-06-16 | 初始版本，从原始文档拆分 |
| V1.1 | 2026-06-17 | 优化调用流程图，补充异常处理路径 |

## 设备信息

| 项目 | 内容 |
|------|------|
| 设备类型 | 虹膜仪 |
| 品牌 | 眼神 |
| 型号 | ECX101-80 |
| DIS 接口前缀 | DEV_Iriscope |

## 调用流程

```
Open → Capture → (获取虹膜采集结果) → Stop → Close
```

> Open 时返回 video_url，可获取虹膜仪预览视频流，流程请参阅通用协议层-视频流获取。

## 接口列表

### 1. 打开虹膜仪（Open）

通过本条指令上层应用可以打开虹膜仪。获取视频流流程请参阅通用协议层-视频流获取。

#### 请求参数

请求示例：

```json
{
  "seq": "DEV_Iriscope_Open_${uuid}",
  "cmd": "Open",
  "datetime": "20211201130101",
  "posidx": "00",
  "timeout": "30000",
  "async": "0"
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | DEV_Iriscope_Open_${uuid} |
| cmd | string | 是 | 固定为"Open" |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| timeout | string | 是 | 超时时间(ms) |
| async | string | 是 | 是否异步（默认0:同步）；0：同步；1：异步 |

#### 返回参数

返回示例：

```json
{
  "seq": "DEV_Iriscope_Open_${uuid}",
  "cmd": "Open",
  "datetime": "20211201130102",
  "code": "0",
  "msg": "Success",
  "suggest": "",
  "posidx": "00",
  "DllVersion": "V6.24.703.1",
  "data": {
    "video_url": [
      {
        "00": "ws://127.0.0.1:62383/dis/hi_video"
      }
    ]
  }
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | 同下发的 seq |
| cmd | string | 是 | 同下发的 cmd |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| code | string | 是 | 参照通用返回码 / 虹膜仪返回码 |
| msg | string | 否 | 提示信息 |
| suggest | string | 否 | 建议 |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| DllVersion | string | 否 | 外设库版本号 |
| data | object | 否 | 返回数据 |
| ↳ video_url | 数组 | 是 | 虹膜仪预览视频的数据流地址 |

---

### 2. 虹膜采集（Capture）

通过本条指令上层应用可以使用虹膜仪开始采集虹膜数据。

#### 请求参数

请求示例：

```json
{
  "seq": "DEV_Iriscope_Capture_${uuid}",
  "cmd": "Capture",
  "datetime": "20211201130101",
  "param": {
    "PersoIris": "D:/Iriscope/",
    "EyeFlag": "3",
    "Compress": "80"
  },
  "posidx": "00",
  "async": "0"
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | DEV_Iriscope_Capture_${uuid} |
| cmd | string | 是 | 固定为"Capture" |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| timeout | string | 是 | 超时时间(ms) |
| async | string | 是 | 是否异步（默认0:同步）；0：同步；1：异步 |
| param | object | 是 | 参数对象 |
| ↳ PersoIris | string | 是 | 虹膜数据保存路径 |
| ↳ EyeFlag | string | 是 | 眼睛标志；"1"：左眼；"2"：右眼；"3"：双眼 |
| ↳ Compress | string | 否 | 压缩质量；默认"80" |

#### 返回参数

返回示例：

```json
{
  "seq": "DEV_Iriscope_Capture_${uuid}",
  "cmd": "Capture",
  "datetime": "20211201130102",
  "code": "0",
  "msg": "Success",
  "suggest": "",
  "data": {
    "Iris_model": "3",
    "PersoIris": "D:/Iriscope/Iris.jpg",
    "Iris_Feature": "D:/Iriscope/Iris_Feature.txt",
    "Iris_Crop_Right": "D:/Iriscope/Iris_Crop_Right.jpg",
    "Iris_Feature_Right": "D:/Iriscope/Iris_Feature_Right.txt",
    "Iris_Score_Right": "80",
    "Iris_Crop_Left": "D:/Iriscope/Iris_Crop_Left.jpg",
    "Iris_Feature_Left": "D:/Iriscope/Iris_Feature_Left.txt",
    "Iris_Score_Left": "80"
  }
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | 同下发的 seq |
| cmd | string | 是 | 同下发的 cmd |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| code | string | 是 | 参照通用返回码 / 虹膜仪返回码 |
| msg | string | 否 | 提示信息 |
| suggest | string | 否 | 建议 |
| data | object | 否 | 返回数据 |
| ↳ Iris_model | string | 是 | 虹膜模式；"3"：双眼 |
| ↳ PersoIris | string | 是 | 虹膜图片保存路径 |
| ↳ Iris_Feature | string | 是 | 虹膜特征值文件路径 |
| ↳ Iris_Crop_Right | string | 否 | 右眼裁剪图片路径 |
| ↳ Iris_Feature_Right | string | 否 | 右眼特征值文件路径 |
| ↳ Iris_Score_Right | string | 否 | 右眼虹膜质量分值 |
| ↳ Iris_Crop_Left | string | 否 | 左眼裁剪图片路径 |
| ↳ Iris_Feature_Left | string | 否 | 左眼特征值文件路径 |
| ↳ Iris_Score_Left | string | 否 | 左眼虹膜质量分值 |

---

### 3. 停止采集（Stop）

当上层业务需要中止当前处理流程时，可调用该指令结束正在进行的操作。

#### 请求参数

请求示例：

```json
{
  "seq": "DEV_Iriscope_Stop_${uuid}",
  "cmd": "Stop",
  "datetime": "20211201130101",
  "async": "1",
  "timeout": "30000",
  "posidx": "00"
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | DEV_Iriscope_Stop_${uuid} |
| cmd | string | 是 | 固定为"Stop" |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| timeout | string | 是 | 超时时间(ms) |
| async | string | 是 | 是否异步（建议为1）；0：同步；1：异步 |

#### 返回参数

返回示例：

```json
{
  "seq": "DEV_Iriscope_Stop_${uuid}",
  "cmd": "Stop",
  "datetime": "20211201130102",
  "code": "0",
  "msg": "Success",
  "suggest": "",
  "posidx": "00",
  "DllVersion": "V6.24.703.1"
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | 同下发的 seq |
| cmd | string | 是 | 同下发的 cmd |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| code | string | 是 | 参照通用返回码 / 虹膜仪返回码 |
| msg | string | 否 | 提示信息 |
| suggest | string | 否 | 建议 |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| DllVersion | string | 否 | 外设库版本号 |

---

### 4. 关闭虹膜仪（Close）

通过本条指令上层应用可以关闭虹膜仪。

#### 请求参数

请求示例：

```json
{
  "seq": "DEV_Iriscope_Close_${uuid}",
  "cmd": "Close",
  "datetime": "20211201130101",
  "timeout": "30000",
  "posidx": "00",
  "async": "0"
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | DEV_Iriscope_Close_${uuid} |
| cmd | string | 是 | 固定为"Close" |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |
| timeout | string | 是 | 超时时间(ms) |
| async | string | 是 | 是否异步（默认0:同步）；0：同步；1：异步 |

#### 返回参数

返回示例：

```json
{
  "seq": "DEV_Iriscope_Close_${uuid}",
  "cmd": "Close",
  "datetime": "20211201130102",
  "code": "0",
  "posidx": "00",
  "msg": "Success",
  "suggest": ""
}
```

参数说明：

| 参数名称 | 格式 | 是否必填 | 参数说明 |
|----------|------|----------|----------|
| seq | string | 是 | 同下发的 seq |
| cmd | string | 是 | 同下发的 cmd |
| datetime | string | 是 | 指令的下发时间，格式：YYYYMMddHHmmss |
| code | string | 是 | 参照通用返回码 / 虹膜仪返回码 |
| msg | string | 否 | 提示信息 |
| suggest | string | 否 | 建议 |
| posidx | string | 是 | 多个同款设备的工位号；"00"~"99" |

## 错误码

| 序号 | 错误码 | 含义 |
|------|--------|------|
| 1 | 15700001 | 超时 |
| 2 | 15700003 | 指针无效 |
| 3 | 15700004 | 此服务功能暂不支持 |
| 4 | 15700005 | 内存不足 |
| 5 | 15700006 | 线程恢复失败 |
| 6 | 15700007 | 线程创建失败 |
| 7 | 15700008 | Event 创建失败 |
| 8 | 15700009 | 命令执行失败 |
| 9 | 15700010 | 命令执行超时 |
| 10 | 99999999 | 未知错误 |
| 11 | 15700101 | 设备未打开 |
| 12 | 15700107 | 设备繁忙 |
| 13 | 15700109 | 已经打开设备，已初始化 |
| 14 | 15700110 | 设备不存在 |
| 15 | 15700113 | 设备通讯失败 |
| 16 | 15700114 | 设备操作失败 |
| 17 | 15700115 | 设备不支持 |
| 18 | 15700116 | 设备句柄无效 |
| 19 | 15700117 | 初始化失败 |

> 通用返回码（0~1037）请参阅 [通用返回码](../00-通用协议层/06-通用返回码.md)